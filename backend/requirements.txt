# FastAPI Backend Dependencies for Outbreak Sentinel
# Minimal setup compatible with Python 3.13

# Core FastAPI framework
fastapi
uvicorn[standard]

# Data validation and serialization
pydantic

# Date/time handling (built into Python, but explicit for clarity)
python-dateutil

# HTTP client for external APIs (future use)
httpx
requests

# Environment and configuration
python-dotenv

# Development and testing
pytest
pytest-asyncio

# Optional: For future live data integration (install separately when needed)
# pandas>=2.2.0  # Install only if needed: pip install pandas
# numpy>=1.26.0  # Install only if needed: pip install numpy
# pytrends>=4.9.0
# tweepy>=4.14.0
